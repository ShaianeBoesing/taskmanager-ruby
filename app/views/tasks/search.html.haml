%p{:style => "color: green"}= notice
%h1
    = "Tasks of " + @project.name
%small
    %p
        = @project.init.strftime("%d/%m/%Y") 
        a
        = @project.end.strftime("%d/%m/%Y")    
= link_to "New task", new_task_path(:project_id => @project)
= render "task" 
= link_to "Back", :back, class: "btn btn-default"
